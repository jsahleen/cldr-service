#!/bin/sh
yarn add cldr-core@${CLDR_VERSION}
yarn add cldr-localenames-${CLDR_TIER}@${CLDR_VERSION} 
yarn add cldr-numbers-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-dates-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-buddhist-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-chinese-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-coptic-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-dangi-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-ethiopic-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-hebrew-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-indian-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-islamic-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-japanese-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-persian-${CLDR_TIER}@${CLDR_VERSION}
yarn add cldr-cal-roc-${CLDR_TIER}@${CLDR_VERSION}
