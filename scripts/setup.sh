#!/bin/sh
yarn upgrade cldr-core@${CLDR_VERSION}
yarn upgrade cldr-localenames-${CLDR_TIER}@${CLDR_VERSION} 
yarn upgrade cldr-numbers-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-dates-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-units-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-buddhist-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-chinese-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-coptic-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-dangi-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-ethiopic-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-hebrew-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-indian-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-islamic-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-japanese-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-persian-${CLDR_TIER}@${CLDR_VERSION}
yarn upgrade cldr-cal-roc-${CLDR_TIER}@${CLDR_VERSION}
